use crate::token::Token;

// TODO
pub struct Scanner;

// TODO
impl Scanner {
    pub fn new(content: Vec<char>) -> Self {
        Scanner
    }

    pub fn scan_tokens(&mut self) -> Vec<Token> {
        vec![]
    }
}
